@model IEnumerable<DnD___Campaign_Dashboard_v._1.Models.MagicItemModelsApi.MagicItemResultModel>
@{ ViewBag.Title = "MagicItems";
    Layout = "~/Views/Shared/_Layout.cshtml"; }

<h2>Magic Items</h2>
<table id="MagicItems" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Magic Item</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>


@section scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            $.ajax({
                url: 'https://www.dnd5eapi.co/api/magic-items',
                type: 'GET',
                dataType: 'json',
                success: function (data) {
                    bindtoDatatable(data.results);
                }
            });
        });
        function bindtoDatatable(data) {
            var table = $('#MagicItems').dataTable({
                "bAutoWidth": true,
                "aaData": data,
                "columns": [

                    {
                        "data": "name"
                    },
                    {
                        "data": "index",
                        render: function (data) {
                            return "<a href='../MagicItems/Details/" + data + "'>Details</a>"
                        }
                    }
                ]
            });
        }
    </script>
}
