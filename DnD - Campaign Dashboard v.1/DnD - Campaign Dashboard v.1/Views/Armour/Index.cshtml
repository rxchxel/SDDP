
@{ ViewBag.Title = "Armours";
    Layout = "~/Views/Shared/_Layout.cshtml"; }

<h2>Armours</h2>
<table id="armors" class="table table-bordered table-hover">
    <thead>
        <tr>

            <th>Armor</th>
            <th></th>

        </tr>
    </thead>
    <tbody>
    </tbody>
</table>

@section scripts
    {
    <script type="text/javascript">
        $(document).ready(function () {
            $.ajax({
                url: 'https://www.dnd5eapi.co/api/equipment-categories/armor',
                type: 'GET',
                dataType: 'json',
                success: function (data) {
                    bindtoDatatable(data.equipment);
                }
            });
        });
        function bindtoDatatable(data) {
            var table = $('#armors').dataTable({
                "bAutoWidth": true,
                "aaData": data,
                "columns": [

                    {
                        "data": "name"
                    },
                    {
                        "data": "index",
                        render: function (data) {
                            return "<a href='../Armour/Details/" + data + "'>Details</a>"
                        }
                    }
                ]
            });
        }
    </script>
}
