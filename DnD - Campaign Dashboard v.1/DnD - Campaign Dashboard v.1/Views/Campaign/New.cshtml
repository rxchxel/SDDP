@model DnD___Campaign_Dashboard_v._1.Models.CampaignViewModel
@{
    ViewBag.Title = "New Campaign";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>New Campaign</h2>
@using (Html.BeginForm("Save", "Campaign"))
{
    @Html.ValidationSummary(true, "Please fix the following errors!")
    <div class="form-group">
        @Html.LabelFor(c => c.Campaign.Name)
        @Html.TextBoxFor(c => c.Campaign.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Campaign.Name)
    </div>
    <div class="form-group">
        @Html.LabelFor(c => c.Campaign.Story)
        @Html.TextAreaFor(c => c.Campaign.Story, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Campaign.Story)
    </div>
    @*<div class="form-group">
        @Html.LabelFor(c => c.Campaign.Characters)
        @Html.DropDownListFor(m => m.Campaign.Characters, new SelectList(Model.Characters, "Id", "charname"), "Select Characters", new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Campaign.Characters)
    </div>*@
    @*<div class="form-group">
        @Html.LabelFor(c => c.Campaign.Characters)
        @Html.DropDownListFor(m => m.Campaign.Characters, new SelectList(Model.Characters, "Id", "Name"), "Select Characters", new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Campaign.Characters)
    </div>
    <div class="form-group">
        @Html.LabelFor(c => c.Campaign.Characters)
        @Html.DropDownListFor(m => m.Campaign.Characters, new SelectList(Model.Characters, "Id", "Name"), "Select Characters", new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.Campaign.Characters)
    </div>*@
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>
}

